import { colors } from '../variables/colors'

export const dark = {
  colors: {
    primary: colors.eerieBlack,
    secondary: colors.cultured,
    accent: colors.fireEngineRed,
    alternate: colors.redCrayola,
  },
}
