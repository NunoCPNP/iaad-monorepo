export type initialStateTypes = {
  darkMode: boolean
  sideBarOpen: boolean
  languageSwitch: boolean
}

export const initialState = {
  darkMode: true,
  sideBarOpen: false,
  languageSwitch: true,
}
